# Docker Compose Development Environment
# This file contains ready-to-use development credentials
# Just rename this file to .env and run: docker-compose up -d

ASPNETCORE_ENVIRONMENT=Development
DatabaseProvider=PostgreSQL
DATABASE_URL=Host=postgres;Port=5432;Database=dotnet_study_db;Username=postgres;Password=postgres123;Pooling=true;

# Other connection options (uncomment and modify as needed):

# For local PostgreSQL installation:
# DATABASE_URL=Host=localhost;Port=5432;Database=dotnet_study_db;Username=postgres;Password=postgres123;Pooling=true;

# For API in Docker connecting to host PostgreSQL:
# DATABASE_URL=Host=host.docker.internal;Port=5432;Database=dotnet_study_db;Username=postgres;Password=postgres123;Pooling=true;

# For production (example):
# ASPNETCORE_ENVIRONMENT=Production
# DATABASE_URL=Host=your-host.neon.tech;Database=your_db;Username=your_user;Password=your_password;SSL Mode=Require;Trust Server Certificate=true
